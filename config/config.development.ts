import { DatabaseConfig } from "../src/interface/interface";

export const database: DatabaseConfig = {
  db_host: "localhost",
  db_user: "root",
  db_password: "",
  db_name: "todolist_db",
  db_port: 3306,
};
